<h1><%= Comment %></h1>

<p class="body"><%= @comment.body %></p>
<p>Created at: <%= @comment.created_at %></p>

<p><%= link_to "Delete", comment_path(@comment), method: :delete, data: {confirm: "Are you sure?"}%> | <%= link_to "Edit", edit_comment_path(@comment) %></p>
